const initialState = {
  tracks: [],
  loading: true,
  error: null,
  currentlyPlayingTrack: null,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
